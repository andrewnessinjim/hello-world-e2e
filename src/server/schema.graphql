type Query {
    homePage: HomePage!
}

type HomePage {
    heading: String!
    sections: [Section]!
}

type Section {
    heading: String!
    logos: [Logo]!
}

type Logo {
    logoKey: String!
    toolTip: String!
}