version: '2.4'

services:
  local-hello-world-e2e:
      build:
        context: .
        target: dev
      environment:
        - DB_URI=mongodb://root:root@local-mongo:27017
        - DB_NAMESPACE=helloworld
      volumes:
        - ./:/app/sourcecode
      ports:
        - '3000:3000'
        - '9229:9229'
        - '8080:8080'
      tty: true

  local-mongo:
    image: mongo:5.0.3
    restart: always
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
    tty: true
    volumes:
      - local-mongo-data:/data/db

volumes:
  local-mongo-data: